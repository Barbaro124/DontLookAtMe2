using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightSensorParent : MonoBehaviour
{
    [SerializeField] 
    public GameObject lightSensorLight;

    // Start is called before the first frame update
    void Start()
    {
        //lightSensorLight = GetComponentInChildren
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
